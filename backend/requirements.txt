fastapi>=0.115.0
uvicorn[standard]>=0.32.0
plexapi>=4.15.0
httpx>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
pytest>=8.0.0

# Local NSFW detection
nudenet>=3.4.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# Optional: Freepik stage-1 fast classifier (requires GPU + torch)
# pip install nsfw-image-detector torch
# nsfw-image-detector>=0.1.2
